# Example 2 — Vandermonde (Column Selection, 8-Cycle Free)

Uses a Vandermonde matrix with a selected subset of columns chosen to eliminate
all 8-cycles. Compared against: (1) PEG on the same base matrix, and (2) the
consecutive-column selection from Example 1 as a baseline.
