# Example 3 — BR Code vs. Truncated Vandermonde (Equivalence + BP Applicability)

Two parity-check matrices for the same code: one in standard BR form, one in
our truncated Vandermonde form. Confirms the two representations are equivalent,
and demonstrates that the truncated form is sparse enough to serve as an LDPC
matrix amenable to BP decoding.
